// Tailwind CSS v4 uses CSS-based configuration
// This file is kept for compatibility but config is now in tailwind.css
// See: https://tailwindcss.com/docs/v4-beta

export default {
  // v4 uses @theme in CSS files for configuration
  // Keep this file for any PostCSS plugin compatibility
};
